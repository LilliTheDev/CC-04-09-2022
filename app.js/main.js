//Write a function called repeatStr which repeats the given string string exactly n times.

//Example & Test Cases
//repeatStr(6, "I") // "IIIIII"
//repeatStr(5, "Hello") // "HelloHelloHelloHelloHello"

//Create function that takes in a string and a number as parameters
//Want to concatenate or repeat the string (n) number of times
//Ignore spacing & case sensitivity

function repeatStr(n, s){
  return s.repeat(n);
}
